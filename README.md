# react-at-home

```
 - Mom, can we get React?
 - We have React at home.
React at home:
```

You don't need this, really.

## //TODO:

Use `MutationObserver` to hook DOM insertions (and call component's OnRender()).

Flexible policies for handling various types of nodes (text, comment, etc) in `transform()`.

More flexible policies for handling custom attributes (`x-if`) in components and elements.

Automatically compute inst.nodeValue if vinst has it.

Make component registration less tedious.

Logging policies to customize and filter messages.

Prevent double context updates, including that for removed nodes.

Bound contexts to properties