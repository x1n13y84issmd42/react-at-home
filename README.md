# react-at-home

```
 - Mom, can we get React?
 - We have React at home.
React at home:
```

You don't need this, really.

## //TODO:

Use `MutationObserver` to hook DOM insertions (and call component's OnRender()).

Flexible policies for handling various types of nodes in `transform()`.

More flexible policies for handling custom attributes in components and elements.

Automatically compute inst.nodeValue if vinst has it.

Make component registration less tedious.