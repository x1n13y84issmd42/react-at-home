import { LogGroup } from "./log";
export const handlers = {
    generic: (handlerFactory),
    abort: (handlerFactory),
    afterprint: (handlerFactory),
    animationend: (handlerFactory),
    animationiteration: (handlerFactory),
    animationstart: (handlerFactory),
    beforeprint: (handlerFactory),
    beforeunload: (handlerFactory),
    blur: (handlerFactory),
    canplay: (handlerFactory),
    canplaythrough: (handlerFactory),
    change: (handlerFactory),
    click: (handlerFactory),
    contextmenu: (handlerFactory),
    copy: (handlerFactory),
    cut: (handlerFactory),
    dblclick: (handlerFactory),
    drag: (handlerFactory),
    dragend: (handlerFactory),
    dragenter: (handlerFactory),
    dragleave: (handlerFactory),
    dragover: (handlerFactory),
    dragstart: (handlerFactory),
    drop: (handlerFactory),
    durationchange: (handlerFactory),
    ended: (handlerFactory),
    error: (handlerFactory),
    focus: (handlerFactory),
    focusin: (handlerFactory),
    focusout: (handlerFactory),
    fullscreenchange: (handlerFactory),
    fullscreenerror: (handlerFactory),
    hashchange: (handlerFactory),
    input: (handlerFactory),
    invalid: (handlerFactory),
    keydown: (handlerFactory),
    keypress: (handlerFactory),
    keyup: (handlerFactory),
    load: (handlerFactory),
    loadeddata: (handlerFactory),
    loadedmetadata: (handlerFactory),
    loadstart: (handlerFactory),
    message: (handlerFactory),
    mousedown: (handlerFactory),
    mouseenter: (handlerFactory),
    mouseleave: (handlerFactory),
    mousemove: (handlerFactory),
    mouseover: (handlerFactory),
    mouseout: (handlerFactory),
    mouseup: (handlerFactory),
    mousewheel: (handlerFactory),
    offline: (handlerFactory),
    online: (handlerFactory),
    open: (handlerFactory),
    pagehide: (handlerFactory),
    pageshow: (handlerFactory),
    paste: (handlerFactory),
    pause: (handlerFactory),
    play: (handlerFactory),
    playing: (handlerFactory),
    popstate: (handlerFactory),
    progress: (handlerFactory),
    ratechange: (handlerFactory),
    resize: (handlerFactory),
    reset: (handlerFactory),
    scroll: (handlerFactory),
    search: (handlerFactory),
    seeked: (handlerFactory),
    seeking: (handlerFactory),
    select: (handlerFactory),
    show: (handlerFactory),
    stalled: (handlerFactory),
    storage: (handlerFactory),
    submit: (handlerFactory),
    suspend: (handlerFactory),
    timeupdate: (handlerFactory),
    toggle: (handlerFactory),
    touchcancel: (handlerFactory),
    touchend: (handlerFactory),
    touchmove: (handlerFactory),
    touchstart: (handlerFactory),
    transitionend: (handlerFactory),
    unload: (handlerFactory),
    volumechange: (handlerFactory),
    waiting: (handlerFactory),
    wheel: (handlerFactory),
};
function handlerFactory(fn) {
    return LogGroup.wrap(function (e) {
        return fn.call(this, e);
    }, ' > event', e => e.type);
}
